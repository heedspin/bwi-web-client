.table(ng-cloak)
  header
    .row
      .seven.columns
        .table__title
          h3 {{ options.title }}
      .five.columns
        .table__actions
          .table-expand
            i.icon-popup
          .input--field-inline
              input(ng-model="filterText", placeholder="Search by Name")
              i.icon-search
  .table-wrapper
    table(ng-table="tableParams")
      tr
        th(ng-repeat="col in options.columns", ng-class="col.title") {{ col.title }}
      tr(ng-click="linkTo(data)", ng-repeat="data in options.filteredResults = (options.data | filter:searchAttribute2 | filter:filterText | startYear:selectedStartYear | endYear:selectedEndYear | filter:searchAttribute)")
        td(ng-repeat="col in options.columns") {{ getDataForCol(data, col.key, col.filter) }}
